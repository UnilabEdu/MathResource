import pytest


def test_random_things():
    """
    GIVEN - მოცემულობა / რას ვტესტავს?
    WHEN - გამოყენება / ფუნქციონალი რომელზეც ვტესტავთ
    THEN - მოსალოდნელი შედეგი
    """
    assert True == True
    assert True == 1
    assert None != 1
    assert None is not 1
